// import { utcToZonedTime, format } from 'date-fns-tz'
// import { es } from 'date-fns/locale';

export function formatTimezone(dateString: string | null) {
	// if (!dateString) return '';

	// const utcDateString = `${dateString}Z`;
	// const date = new Date(utcDateString);

	// const timeZone = 'America/Mexico_City';
	// const zonedDate = utcToZonedTime(date, timeZone);
	// // Formatear la fecha en la zona horaria especificada
	// const formattedDate = format(zonedDate, 'dd MMM, yyyy hh:mm:ss aa', { timeZone, locale: es });

	// return formattedDate;

	return ''
}
